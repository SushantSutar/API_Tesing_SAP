# API_Tesing_SAP
This project is an Excel-driven API Testing Tool that:  Reads test cases from an Excel file Fetches, OAuth token dynamically Executes API requests, Validates responses Displays results in UI . It works like a mini Postman + JMeter combined.
